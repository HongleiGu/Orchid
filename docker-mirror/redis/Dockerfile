# Passthrough so Aliyun ACR can rebuild this image on an overseas build
# machine (which can reach Docker Hub) and republish it into ACR, where a
# mainland ECS can pull it at domestic speed.
FROM redis:7-alpine
